# Import functions for controlling robot
import robot_control

# Move forward
robot_control.forward(speed=1, duration=3)
# Turn left
robot_control.turn_left(speed=0.5, duration=2)
# Move forward
robot_control.forward(speed=0.5, duration=1)

